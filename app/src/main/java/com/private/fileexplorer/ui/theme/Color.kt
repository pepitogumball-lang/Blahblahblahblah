package com.private.fileexplorer.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Paleta principal ─────────────────────────────────────────────────────────
val Blue10  = Color(0xFF001F4D)
val Blue40  = Color(0xFF1565C0)
val Blue80  = Color(0xFF90CAF9)
val Blue90  = Color(0xFFBBDEFB)

val Teal40  = Color(0xFF00695C)
val Teal80  = Color(0xFF80CBC4)

val Grey10  = Color(0xFF1A1C1E)
val Grey40  = Color(0xFF546E7A)
val Grey80  = Color(0xFFB0BEC5)
val Grey90  = Color(0xFFECEFF1)

// ─── Colores semánticos de tipos de archivo ───────────────────────────────────
val ColorFolder   = Color(0xFFFFB300)   // Ámbar    — carpetas
val ColorImage    = Color(0xFF43A047)   // Verde    — imágenes
val ColorVideo    = Color(0xFFE53935)   // Rojo     — vídeos
val ColorAudio    = Color(0xFF8E24AA)   // Violeta  — audio
val ColorPdf      = Color(0xFFE53935)   // Rojo     — PDF
val ColorDoc      = Color(0xFF1E88E5)   // Azul     — documentos Office
val ColorArchive  = Color(0xFFFB8C00)   // Naranja  — archivos comprimidos
val ColorCode     = Color(0xFF00ACC1)   // Cian     — código fuente
val ColorText     = Color(0xFF546E7A)   // Gris-azul — texto plano
val ColorGeneric  = Color(0xFF78909C)   // Gris     — otros
